"""Tier 2 GenAI explanation layer (churn/genai/).

Modules added here in subsequent steps:
  client.py     — thin OpenAI-compatible wrapper (Gemini / Groq backends)
  explainer.py  — generates natural-language churn explanations
  rag.py        — retrieval-augmented retention-playbook lookup
"""
